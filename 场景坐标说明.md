# 场景坐标说明

> 记录当前场景中「世界原点、桌子、垫板、收纳盒、螺母」之间的空间关系，
> 澄清「为什么桌子 z 为负、桌上物体 z 为正」这一常见疑问。
> 数据来源：场景编辑器读出的各模型 pose 真值（世界坐标系，单位 m / rad）。

---

## 1. 结论速览

- **世界原点 (0, 0, 0) 不在机器人头顶**，而在 base 底座**下方约 0.75 m** 处。
- 桌上物体（螺母 / 垫板 / 收纳盒）的 z ≈ **+0.26 ~ +0.30 m**，在世界原点**上方**。
- 桌子参考点的 z = **-0.491 m**，在世界原点**下方**。
- 二者一正一负，是因为**世界原点夹在「桌面」和「桌子参考点」之间**，不矛盾。

---

## 2. 各物体世界坐标真值

| 物体 | 位置 (x, y, z) m | 姿态 (r, p, y) rad | 说明 |
|---|---|---|---|
| 桌子 table_27_11 | (0, 0, **-0.491**) | (0, 0, 1.57) | z 是**网格参考点**，不是桌面 |
| 垫板 | (-0.2977, -0.1122, **0.2636**) | (-1.57, 0, 0) | |
| 收纳盒 | (-0.3104, 0.2586, **0.2996**) | (0, 0, 1.57) | |
| 螺母 A | (-0.2286, -0.0999, **0.2815**) | (0, 0, 0.5233) | |
| 螺母 B | (-0.3413, -0.1710, **0.2806**) | (0, 0, 0.5233) | |
| 螺母 C | (-0.2975, -0.0527, **0.2868**) | (0, 0, 0.5233) | |
| base 底座 | (-0.6816, 0, **0.752**) | (0, 0, 0) | 底座在世界原点上方 |

> 桌面上物体的 z 都落在 0.26 ~ 0.30 这一带，说明**桌面实际高度 ≈ 0.28 m**。

---

## 3. 为什么桌子 z 为负、桌上物体 z 为正

关键点：**pose 里的 `(x, y, z)` 是「模型局部坐标系原点」在世界中的位置，不是物体表面 / 接触面的高度。**

每个模型的网格在建模时定义了各自的局部原点，不同模型原点位置不同：

| 物体 | 网格原点大致位置 | 含义 |
|---|---|---|
| 桌子 | 桌腿底部一带 | z=-0.491 是**参考点**，不是桌面 |
| 垫板 / 螺母 / 收纳盒 | 在本体上 | z 直接反映本体高度 |

桌子的参考点在世界原点下方，桌上物体的参考点在原点上方，两者在原点两侧，所以符号相反。

---

## 4. z 轴高度关系图

```
 z ≈ +1.05   头部相机（底座上方约 0.29 m 的相机部件）
 z ≈ +0.75   base 底座
 z ≈ +0.28   桌面 / 螺母 / 垫板 / 收纳盒
 ─────────────────── z = 0  ← 世界原点在这里
 z ≈ -0.49   桌子网格参考点（桌腿底部一带）
```

- 世界原点在桌面下方约 0.28 m、底座下方约 0.75 m。
- 螺母和桌子**确实都在相机下方**，但两者相对世界原点一上一下，所以一正一负。

---

## 5. 常见误区

1. **「世界原点在机器人头顶」** —— 错误。原点在底座**下方** 0.75 m 处。
2. **「桌上物体离原点 = 0.491 - 0.28 = 0.211」** —— 错误。物体 pose 的 z 值**本身就是**它离原点的距离（≈ 0.28 m），无需用桌子换算。
3. **「物体高度需要桌子参与换算」** —— 不必。thing 的 pose 是相对世界的绝对坐标，直接读即可。
4. **「桌子高度 = 0.28」** —— 错误。0.28 是**桌面高度**（由桌上物体 z 推断）；桌子「参考点 → 桌面」的距离 ≈ 0.28 + 0.491 = **0.77 m**（该值为反推估算，前提是物体直接置于桌面、且参考点在桌腿底）。

---

## 6. 正确关系式（如需用桌子反推）

```
桌上物体 z = 桌子参考点 z + (参考点 → 桌面的距离)
          = (-0.491) + 0.77
          ≈ 0.28   ✓
```

但最直接的做法仍是：**物体 pose 的 z 值就是它离世界原点的高度**，直接读标注即可。
