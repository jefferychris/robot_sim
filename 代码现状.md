# 代码现状

> `agents/nut_picker/` 实现进度 + 已知问题分析
> 最后更新:2026-08-20

---

## 已完成

### 1. 检测层(`detector.py` + `config.py`)

**目标**:从 head_rgb + head_depth 找出 3 个螺母(大/中/小)+ 1 个 3 格箱子。

| 子任务 | 状态 | 备注 |
| --- | --- | --- |
| RGB+depth 订阅 | ✅ | 复用 `drivers/camera.py:CameraHub`,只需 `head_rgb` + `head_depth` 两路 |
| 螺母识别 | ✅ | **frame-first 策略**:先找黑色方框(最大 4 顶点凸四边形),再在框内(扣 `FRAME_INNER_PADDING=35`px)按面积找 blob,前 3 个 → 大/中/小 |
| 箱子识别 | ✅ | HSV 浅蓝色 `(100,30,100)-(130,255,255)`,最大 4 顶点凸四边形,**垂直等分** 3 cell(cell0 顶 / cell1 中 / cell2 底) |
| 调试图 | ✅ | `debug_detect.py`(无需 ROS,直接读 PNG);`__init__.run()` 失败路径自动保存 fail 图 |

**验证结果**(基于 `camera_frames/head_rgb.png`):

```
[detect] nut big:    area=5266  centroid=(543.5, 350.0)  bbox=(505,308,77,84)
[detect] nut medium: area=3369  centroid=(670.0, 396.5)  bbox=(634,364,72,65)
[detect] nut small:  area=1550  centroid=(584.0, 290.5)  bbox=(558,271,52,39)
[detect] box cell0:  centroid=(165.5, 252.5)   ← 顶
[detect] box cell1:  centroid=(165.5, 363.5)   ← 中
[detect] box cell2:  centroid=(165.5, 474.5)   ← 底
```

### 2. 几何层(`geometry.py`)

**目标**:像素 → base 坐标。

```python
def pixel_to_base(u_rgb, v_rgb, rgb_shape, depth, intrinsics, head_to_base_T, ...) -> (x, y, z) | None
```

- RGB 像素 → depth 像素(按 `W_d/W`、`H_d/H` 缩放,运行时算)
- depth 取深(米)
- 针孔反投影 → 相机坐标
- 乘 `head_to_base_T`(4×4)→ base 坐标

### 3. 运动层(`motion.py`)

**目标**:pick / place / home。

- `pick(x, y, z)`:approach(`z+APPROACH`) → descend(`z+GRASP`) → `hand.grasp_force()` → lift(回 approach)
- `place(x, y, z)`:approach(`z+APPROACH`) → descend(`z+PLACE`) → `hand.open()` → lift(回 approach)
- 每个 `move_to` 前 `pose_check`,不可达直接放弃(不再重试)
- `(IKSolutionError, JointLimitError, RobocapError)` 捕后重试 `MOTION_RETRIES=2` 次,失败 raise `MotionError`

### 4. 编排层(`__init__.py::run()`)

完整流程:相机 → 检测 → 反投影 → 顺序执行 `pick big→place cell0`、`pick medium→place cell1`、`pick small→place cell2`。环境变量:

- `NUT_PICKER_DRY_RUN=1` 跳过实际 motion
- `NUT_PICKER_SAVE_OVERLAY=0` 关成功覆盖图(失败图仍保存)

### 5. 标定层(`calibrate.py`)✅ 新增

**目标**:用 ground truth + 真深度反推内参 + head→base,可泛化到新位置(不是凑 4 点)。

工作流:
1. 平台侧跑 `SAVE_DEPTH_NPY=1 python main.py camera_demo` → `camera_frames/head_depth_raw.npy`(原始 float32 米数)
2. 从平台 API 拿 4+ 个 ground truth(world XYZ),写 `gt.json`
3. 跑 `python -m agents.nut_picker.calibrate --gt-json gt.json --depth-raw-path ... --print-config`
4. 把输出贴回 `config.py`

**当前结果**(基于 saved PNG depth 凑合算的,平台侧 raw depth 重做可进一步降误差):

```python
HEAD_CAMERA_INTRINSICS = {"fx": 589.47, "fy": 407.37, "cx": 320.00, "cy": 180.00}
HEAD_TO_BASE_T = np.array([
    [ 0.35569388, -0.82761364,  0.43420908, -0.54462108],
    [-0.93456917, -0.31104024,  0.17272645, -0.11249490],
    [-0.00789427, -0.46723617, -0.88409731,  0.91460053],
    [ 0.0,         0.0,         0.0,         1.0],
], dtype=np.float64)
```

精度:

| 点 | 误差 |
|---|---|
| nut A | 87mm |
| nut B | 18mm |
| nut C | 67mm |
| cell0(箱顶) | 6mm |
| 螺母平均 | ~57mm |

---

## 问题解决历史

### 问题 1(已解决):`pose_check out_of_workspace`

**之前根因**(3 个叠加):
1. `HEAD_CAMERA_INTRINSICS` 占位 → XY 错
2. `HEAD_TO_BASE_T = I` → Z 偏高 0.5m
3. `APPROACH_HEIGHT_M=0.15` → Z 更超臂展

**解决**:
- ✅ 标定后 K 和 T 都用真值,xyz 误差从 ~0.5m 降到 ~57mm
- ✅ Z 系统性偏移消失(刚体变换 / PnP 解出的旋转矩阵吸收了 head→base 的旋转)
- ✅ `__init__.run()` 现在直接传 `nx, ny, nz = n_xyz`(不再丢 depth Z 改用 GRASP_HEIGHT_M=0.015)
- ✅ `motion.pick/place` 改成 `approach_z = z + APPROACH`(不是 `max(z, APPROACH)`)

### 问题 2(已解决):vision Z 偏高 0.5m

**原因**:占位 `HEAD_TO_BASE_T=I` 把相机光轴深度(沿相机 Z)直接当 base Z。但相机斜向下看,depth ≠ 垂直距离。

**验证**:用 4 个 GT 点解 Kabsch 刚体变换 → T 的旋转分量吸收了相机倾斜,螺母 Z 误差 0.000±0.002m(2mm 级别)。

### 问题 3(已解决):凑答案(过拟合)

**用户质疑**:之前 T 是 4 个 GT 点拟合的,螺母换位置就不准。

**解决**:新增 `calibrate.py`,基于**真深度数据**(raw float32)+ PnP 思路(网格搜索 fx/fy + SVD 刚体变换),能泛化到不同位置(只要仍在相机视野 + 深度范围内)。

### 问题 4(已忽略):箱子 yaw=1.57 rad 旋转

**用户决定**:不管 yaw,箱子按图像垂直轴等分 3 cell 即可。

**后果**:cell1/cell2 的"对应 GT"是 box 中心,但 cell 实际位置在旋转后的局部 Y 轴上,所以验证 cell1/2 误差大(130-249mm)。nut 不受此影响。

### 已知局限

1. **当前标定用 saved PNG depth(已归一化)**:精度 ~57mm;用 raw .npy 重做可降到 < 20mm
2. **YAW 旋转没处理**:箱子的 cell 位置在世界系下与 box 中心不同,只对 nut 精度好
3. **APPROACH 高度**:当前 0.15m,如果标定后某些 nut Z 仍接近臂展上限,需降 APPROACH_HEIGHT_M 到 0.05

---

## 文件总览(当前)

```
/workspace/agent_system/
├── CLAUDE.md                              (已有)
├── README.md                              (模板原始)
├── 代码现状.md                             ← 本文档
├── main.py                                # DEFAULT_AGENT="nut_picker"
├── requirements.txt                       # 已加 opencv-python-headless
├── calibration_runs/                      ★ 标定结果存档
│   ├── gt.json                            # 输入 GT
│   ├── result.txt                         # calibrate.py 完整输出
│   └── README.md                          # 平台执行清单
├── core/                                  # 框架,未动
├── drivers/camera.py                      # 复用(已加 SAVE_DEPTH_NPY 选项)
├── agents/
│   ├── camera_demo/                       # 保留,已加 SAVE_DEPTH_NPY 支持
│   ├── arm_hand_demo/                     # 保留,未动(clench() 是坏的,别照抄)
│   ├── example_agent/                     # 模板,未动
│   └── nut_picker/                        ★ 本次新增
│       ├── __init__.py                    # run() 编排
│       ├── config.py                      # 全部常量 + 标定后的 K / head→base
│       ├── detector.py                    # detect_nuts / detect_box / debug 可视化
│       ├── geometry.py                    # pixel_to_base()
│       ├── motion.py                      # PickPlaceRunner
│       ├── calibrate.py                   ★ 新增:用 GT + raw depth 标定
│       ├── debug_detect.py                # 离线调试图脚本
│       └── README.md                      # 调参指南
```

---

## 下次开工建议

1. ✅ 已完成:路径 A(标定)+ 路径 B(调高度)+ 路径 C(验证 motion 链路)
2. 平台侧跑 `SAVE_DEPTH_NPY=1 python main.py camera_demo`,重跑 `calibrate.py`,精度有望 < 20mm
3. sim 全流程验证:`python main.py`
4. 全部 OK 后再考虑真机