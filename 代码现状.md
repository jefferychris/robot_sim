# 代码现状

> `agents/nut_picker/` 实现进度 + 已知问题分析
> 最后更新:2026-08-19

---

## 已完成

### 1. 检测层(`detector.py` + `config.py`)

**目标**:从 head_rgb + head_depth 找出 3 个螺母(大/中/小)+ 1 个 3 格箱子。

| 子任务 | 状态 | 备注 |
| --- | --- | --- |
| RGB+depth 订阅 | ✅ | 复用 `drivers/camera.py:CameraHub`,只需 `head_rgb` + `head_depth` 两路 |
| 螺母识别 | ✅ | **frame-first 策略**:先找黑色方框(最大 4 顶点凸四边形),再在框内(扣 `FRAME_INNER_PADDING=35`px)按面积找 blob,前 3 个 → 大/中/小 |
| 箱子识别 | ✅ | HSV 浅蓝色 `(100,30,100)-(130,255,255)`,最大 4 顶点凸四边形,**垂直等分** 3 cell(cell0 顶 / cell1 中 / cell2 底) |
| 调试图 | ✅ | `debug_detect.py`(无需 ROS,直接读 PNG);`__init__.run()` 失败路径自动保存 fail 图 |

**验证结果**(基于 `camera_frames/head_rgb.png`):

```
[detect] nut big:    area=5266  centroid=(543.5, 350.0)  bbox=(505,308,77,84)
[detect] nut medium: area=3369  centroid=(670.0, 396.5)  bbox=(634,364,72,65)
[detect] nut small:  area=1550  centroid=(584.0, 290.5)  bbox=(558,271,52,39)
[detect] box cell0:  centroid=(165.5, 252.5)   ← 顶
[detect] box cell1:  centroid=(165.5, 363.5)   ← 中
[detect] box cell2:  centroid=(165.5, 474.5)   ← 底
```

### 2. 几何层(`geometry.py`)

**目标**:像素 → base 坐标。

```python
def pixel_to_base(u_rgb, v_rgb, rgb_shape, depth, intrinsics, head_to_base_T, ...) -> (x, y, z) | None
```

- RGB 像素 → depth 像素(按 `W_d/W`、`H_d/H` 缩放,运行时算)
- depth 取深(米)
- 针孔反投影 → 相机坐标
- 乘 `head_to_base_T`(4×4)→ base 坐标

### 3. 运动层(`motion.py`)

**目标**:pick / place / home。

- `pick(x, y, z)`:approach(`z+APPROACH`) → descend(`z+GRASP`) → `hand.grasp_force()` → lift(回 approach)
- `place(x, y, z)`:approach(`z+APPROACH`) → descend(`z+PLACE`) → `hand.open()` → lift(回 approach)
- 每个 `move_to` 前 `pose_check`,不可达直接放弃(不再重试)
- `(IKSolutionError, JointLimitError, RobocapError)` 捕后重试 `MOTION_RETRIES=2` 次,失败 raise `MotionError`

### 4. 编排层(`__init__.py::run()`)

完整流程:相机 → 检测 → 反投影 → 顺序执行 `pick big→place cell0`、`pick medium→place cell1`、`pick small→place cell2`。环境变量:

- `NUT_PICKER_DRY_RUN=1` 跳过实际 motion
- `NUT_PICKER_SAVE_OVERLAY=0` 关成功覆盖图(失败图仍保存)

---

## 当前遇到的问题:`pose_check out_of_workspace`

### 现象

日志片段:

```
[plan] big @ (0.054, 0.068, 0.785) → cell0 @ (-0.285, -0.016, 0.836)
[plan] medium @ (0.158, 0.105, 0.765) → cell1 @ (-0.267, 0.079, 0.782)
[plan] small @ (0.092, 0.018, 0.813) → cell2 @ (-0.251, 0.163, 0.735)

[motion] pose_check 不可达 @(0.054, 0.068, 0.935): out_of_workspace  ← approach
[motion] pose_check 不可达 @(0.158, 0.105, 0.915): out_of_workspace
[motion] pose_check 不可达 @(0.092, 0.018, 0.963): out_of_workspace
```

### 根因分析(3 个互相叠加的因子)

#### ① **未标定的相机内参和 head→base 变换**

`config.py` 里:

```python
HEAD_CAMERA_INTRINSICS = {"fx": 615.0, "fy": 615.0, "cx": 320.0, "cy": 180.0}  # 占位
HEAD_TO_BASE_T         = np.eye(4)                                             # 单位矩阵(假设相机帧≈base 帧)
```

这意味着 depth 值(米)被**直接当成 base 坐标系的 Z**。但:

- head_rgb 的真实分辨率 1920×1080,保存到 PNG 是 960×540(下采样了一半)
- 保存的 depth PNG 是 normalized 灰度,**不是真实深度**(无法从灰度恢复米数)
- 真实 depth 必须从**实时** `head_depth` 话题拿,原始 `float32` 才是米
- 没有标定,反投影出来的 XY 也只是"按占位内参算出来的"近似值

所以日志里的 `(0.054, 0.068, 0.785)` **完全不是真实的 base 系坐标**,只是占位内参与单位变换算出来的"假象"。

#### ② **LinkerArmA7 的工作空间限制**

7-DOF 机械臂,典型最大伸展半径 ~0.85m。base_link 在地面,base 系 Z 朝上。

- 日志里所有 nut 的 Z 在 0.7~0.85m 范围,**接近臂展极限**
- 所有 XY 都在 ±0.3m 范围,**几乎在 base 正上方**
- 这种配置等于"臂几乎直立 + 微小横向偏移",**接近奇异位姿**
- `APPROACH_HEIGHT_M=0.15` 把 Z 抬到 0.93~0.96m,**完全超出臂展**

#### ③ **APPORACH 偏移加剧问题**

加了 `+ APPROACH_HEIGHT_M` 之后,Z 进一步抬高,但臂展不够长,不可达。

如果**没有标定**(XY 是错的),即使 APPROACH 调小到 0,Z 也只是从错误的"假象 base Z"出发,仍不可达。

### 结论

**真因不是 motion 代码,是相机未标定 + 场景几何约束**。具体说:

1. 不标定 → 算出来的 base 坐标全是假的
2. 即使标定了,APPROACH 抬升 0.15m 也可能让 Z 超出臂展上限
3. 当前所有 nut/box 在 base 上方 0.7~0.85m,臂展几乎没有横向容差

---

## 修复路径(下一步候选,不立即改)

按重要性排序:

### 路径 A:**先标定**,再调高度

这是最根本的修复。步骤:

1. **填相机内参**:平台启动后 `ros2 topic list | grep camera_info`,echo 出 `K` 矩阵填到 `config.py`
2. **填 head→base 4×4**:用平台手眼标定;或用已知物体在已知 base 坐标的世界,反推 4×4
3. **跑 `NUT_PICKER_DRY_RUN=1 python main.py`**,检查算出来的 (x, y, z) 是否在臂展范围内:
   - Z 应 ≤ 0.85m(臂展上限)
   - XY 应在 (0.2, 0.6) 半径内(sweet spot)
   - 如果 Z 还是太高,说明相机到工作平面的距离算错了 → 内参或外参不对

### 路径 B:**如果标定后 Z 仍超限**

- 把 `APPROACH_HEIGHT_M` 从 0.15 降到 0.05(只留 5cm 余量)
- 把 `GRASP_HEIGHT_M` 从 0.015 改 0.005(更贴近 nut 表面)
- 或者改工作场景:让 nut 离 base 近一些(head 相机抬高一些 → nut 在世界 Z 更低)

### 路径 C:**先验证 motion 链路本身是通的**

写一个静态测试用例:让机械臂走到一个**已知可达的坐标**(例如 `(0.3, 0.0, 0.3)`,臂展正中),验证 `pose_check`/`move_to` 调用本身没问题。这样把"标定问题"和"motion 代码问题"分开。

### 路径 D:**临时绕过,验证流程**

如果只想验证 pick→place 流程走得通(不实际抓螺母),可以在 `__init__.run()` 里给每个目标加一个**坐标偏移/缩放**,让它们落在臂展内:

```python
# 临时:把目标坐标线性缩放到臂展内,验证逻辑
nx, ny, nz = n_xyz
nx, ny = nx * 0.5, ny * 0.5  # XY 缩半
nz = min(nz, 0.5)            # Z 上限 0.5
```

这样能验证反投影+motion 链路是否运转正常,但抓不到真正的螺母。

---

## 文件总览(当前)

```
/workspace/agent_system/
├── CLAUDE.md                              (已有)
├── README.md                              (模板原始)
├── 代码现状.md                             ← 本文档
├── main.py                                # DEFAULT_AGENT="nut_picker"
├── requirements.txt                       # 已加 opencv-python-headless
├── core/                                  # 框架,未动
├── drivers/camera.py                      # 复用,未动
├── agents/
│   ├── camera_demo/                       # 保留,未动
│   ├── arm_hand_demo/                     # 保留,未动(clench() 是坏的,别照抄)
│   ├── example_agent/                     # 模板,未动
│   └── nut_picker/                        ★ 本次新增
│       ├── __init__.py                    # run() 编排
│       ├── config.py                      # 全部常量(含 TODO 标定项)
│       ├── detector.py                    # detect_nuts / detect_box / debug 可视化
│       ├── geometry.py                    # pixel_to_base()
│       ├── motion.py                      # PickPlaceRunner
│       ├── debug_detect.py                # 离线调试图脚本
│       └── README.md                      # 调参指南
```

---

## 下次开工建议

1. 先做路径 C(静态测试可达坐标),把 motion 代码自身确认无 bug
2. 然后路径 A(标定),拿到真内参和真 head→base
3. 跑 dry-run,看 base 坐标是否落在臂展内
4. 最后 sim 跑全流程
5. 全部 OK 后再考虑真机