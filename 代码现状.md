# 代码现状

> `agents/nut_picker/` 实现进度 + 已知问题
> 最后更新: 2026-08-28

---

## 总体进度

| 模块 | 状态 | 精度/备注 |
|------|------|-----------|
| 视觉检测 (螺母+箱子) | ✅ 完成 | 3 颗螺母 + 3 cell 稳定检测 |
| 坐标系转换 (camera→SDK) | ✅ 完成 | 反投影误差 4-10mm |
| 运动控制 (pick & place) | 🚧 预写完成 | `motion_tmp.py`，逻辑+自检已过，待示教采点标定 |
| 右手 ID | ✅ 完成 | `rcd72e2daf71f064c29aa45d4eeceeca9`，平台验证初始化成功 |
| 桌面高度固定 | ✅ 完成 | `TABLE_Z_M = -0.47`，视觉只出 XY |

---

## 已完成

### 1. 视觉检测 (`detector.py`) ✅

从 head_rgb (960×540) 检测 3 颗六角螺母和 1 个蓝色收纳盒。

**螺母检测**: Canny 边缘 + 多边形近似(5-10 顶点, circularity > 0.5), 按面积排序大/中/小。

**箱子检测**: HSV 蓝色阈值 `(100,30,100)-(130,255,255)`, 最大凸四边形, 垂直等分 3 cell。

检测结果稳定:
```
big:    area=5266  centroid=(543.5, 350.0)
medium: area=3369  centroid=(670.0, 396.5)
small:  area=1550  centroid=(584.0, 290.5)
box cell0/1/2: centroid=(165.5, 252.5/363.5/474.5)
```

### 2. 坐标系转换 (`geometry.py` + `config.py`) ✅

**核心映射**: `世界坐标 → SDK坐标: SDK = (dX, -dY, dZ)`, 其中 `d = world - base_world`

**HEAD_TO_BASE_T**: PnP 标定(4点: 3螺母+箱子, 1.02px 投影误差) + Y 轴翻转(flip_Y)

```python
HEAD_CAMERA_INTRINSICS = {"fx": 554.3, "fy": 554.3, "cx": 320.0, "cy": 180.0}

HEAD_TO_BASE_T = np.array([
    [-0.03685092, -0.89991903,  0.43449712,  0.12444315],
    [ 0.99875594, -0.01855015,  0.04628671, -0.03235092],  # Y翻转
    [ 0.03359431, -0.43566228, -0.89948307,  0.24292208],
    [ 0.0,         0.0,         0.0,         1.0],
])
```

**深度校正**: 深度相机系统性偏大 ~3%, `DEPTH_SCALE_CORRECTION = 0.97`

**验证结果** (dry-run, 2026-08-28):
```
              CV计算                真值              误差
big(C):    (0.388, 0.060, -0.472)  (0.384, 0.053, -0.465)  ~10mm ✓
medium(B): (0.338, 0.169, -0.467)  (0.340, 0.171, -0.471)  ~4mm  ✓
small(A):  (0.447, 0.102, -0.472)  (0.453, 0.100, -0.471)  ~7mm  ✓
```

### 3. 相机 topic namespace ✅

平台 ROS2 topic 带 namespace 前缀 `/gs_c9c18bcde5aab7c1709d7277ff4d828d/`, 已更新到 `camera_demo/config.py`。

### 4. 右臂切换 ✅

已从左臂 `LEFT_ARM_ID` + `LinkerHandO6Left` 改为右臂 `RIGHT_ARM_ID` + `LinkerHandO6Right`。

---

## 待解决

### 1. 示教采点 (唯一阻塞)

`motion_tmp.py` 的 `TEACH_POINTS` 为空，这是它无法实跑的唯一原因。

需人工遥控把右臂摆到「末端悬停于桌面上方、指尖朝下」，在桌面 **4 个铺开的位置**各采一次：

```bash
cd /workspace/agent_system && source /opt/rabo-venvs/agent_system/bin/activate
python3 -c "
from rabo_robocap import LinkerArmA7
from agents.nut_picker import motion_tmp as m
arm = LinkerArmA7(robot_id='r412d237980e3167577d7aece10f7aedb', mode='sim')
print(m.collect_teach_point(arm, note='桌面左前'))
arm.shutdown()
"
```

采点时需同时确认:`move_joints` 能否真的把末端压到 z≈-0.47（整个方案成立的前提）。

### 2. 参数标定 (采点后)

- `GRASP_CLEARANCE_M` — 螺母顶面高度，M27/M33/M45 各不同
- `GRASP_STRENGTH_BY_LABEL` — 三种尺寸的抓握力度
- `get_joint_forces()` 力阈值 — 用于判定是否真抓住

---

## move_to 为什么不可用

2026-08-28 用 `tools/probe_workspace.py` 实测：

```
桌面高度 z=-0.47，三颗螺母在所有姿态下 pose_check 均 out_of_workspace
z 抬到 -0.10 以上才可达
```

但机械臂 home 位末端就在 **z=-0.6082**，比桌面还低 14cm，物理上显然够得到。

**结论**：限制来自 `move_to` 内部 IK 解算器（可能只解特定构型），不是几何不可达。
故笛卡尔路径走不通，必须走关节空间 `move_joints` —— 这是 `motion_tmp.py` 的设计前提。

---

## 关键参数速查

| 参数 | 值 | 来源 |
|------|-----|------|
| base 世界坐标 | (-0.6816, 0, 0.752) | 场景编辑器 |
| SDK 坐标映射 | SDK = (dX, -dY, dZ) | J2=-60° 实测验证 |
| 相机内参 fx=fy | 554.3 | h_fov=1.047rad + 640×360 |
| 相机 pitch | 1.1 rad (63°) | 0.3(link) + 0.8(sensor) |
| 深度校正 | ×0.97 | 3点标定均值 |
| 右臂 ID | r412d237980e3167577d7aece10f7aedb | 已验证 |
| 右手 ID | rcd72e2daf71f064c29aa45d4eeceeca9 | 场景编辑器, 已验证 |
| 桌面高度 | -0.47 | 实测 -0.465/-0.471/-0.471 |
| ROS namespace | gs_c9c18bcde5aab7c1709d7277ff4d828d | ros2 topic list |

---

## 文件总览

```
agents/nut_picker/
├── __init__.py      # run() 编排: 相机→检测→反投影→pick/place
├── config.py        # 全部常量 + 标定参数 (K, T, depth_scale)
├── detector.py      # detect_nuts / detect_box / 可视化
├── geometry.py      # pixel_to_base() 含 depth_scale 校正
├── motion.py        # PickPlaceRunner (现役, 走 move_to)
├── motion_tmp.py    # 【预写未校验】走 move_joints + 示教点雅可比
├── calibrate.py     # PnP 标定工具
└── README.md        # 调参指南

agents/camera_demo/
└── config.py        # 相机 topic (已加 namespace 前缀)

tools/
├── rc.py            # 手动关节控制 (不用 JOINT_SIGN)
├── probe_workspace.py  # 工作空间探测 (pose_check 批量预检)
└── probe_pose.py       # 姿态搜索 (找可行 roll/pitch/yaw)
```

---

## 下一步

1. **示教采点** (阻塞中): 4 个位置采 `TEACH_POINTS`，填入 `motion_tmp.py`
2. **算雅可比**: `fit_jacobian()` 得到 `JACOBIAN_INV`
3. **验证解算精度**: 反解后 `move_joints`，比对实际末端 XY
4. **标定抓取参数**: 螺母顶面高度、三尺寸力度、力反馈阈值
5. **端到端测试** → `motion_tmp.py` 验证通过后合并回 `motion.py`
6. **稳定性验证**: 重复 5-10 次统计成功率

---

## 借鉴来源

`motion_tmp.py` 吸收了 yizhi-robot 项目 (LKLS73-I1 真机，同为 7-DOF) 验证过的工程技巧：

| 技巧 | 作用 |
|------|------|
| 抬起降级重试 `[0.15,0.13,0.10,0.07]` | IK 无解时逐级退让，而非直接失败 |
| 位姿回读校验 (1cm 容差) | 动作后读回实际末端位置，超差重试 |
| 手指指令重发 2 次 | 手部状态读不回，重发保证到达 |
| 失败即恢复 (松手+回 home) | 避免卡在半空挡住视线 |
| 分尺寸抓握力度 | 统一手型抓不稳三种螺母 |

注：关节角数值**不可复用**（不同臂型、不同零位定义），仅借流程与技巧。
